body {
  background-color: #f5f1da;
  font-family: sans-serif;
}

.container {
  padding: 32px;
}

.divider {
  width: 100%
  display: flex;
  justify-content: space-evenly;
  margin: 8px;
  background-color: #F8BD7F;
  padding: 8px;
}