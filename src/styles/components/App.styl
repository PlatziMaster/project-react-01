@import "./Vars.styl"

*
    font-family primary-font
    color rgba(0,0,0,.7 )

.container-sail
    display grid
    grid-template-columns 1fr 1fr
    grid-template-areas "academic skills"\
                        "interest languages"
    column-gap 10px

body
    background radial-gradient(circle, white 0%, gray 200%)
    margin 0
h3,p
    color rgba(0,0,0,.6)
