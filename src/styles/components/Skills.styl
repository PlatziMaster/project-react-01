@import './Vars'

.skills
  grid-area: 4 / 3 / 5 / 5
  background-color: $back-color
  color: $text-color
  display: flex
  justify-content: flex-start;
  align-items: center
  border-radius: $main-border
  box-shadow: $shadow
