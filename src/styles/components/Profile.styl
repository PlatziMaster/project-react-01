.Profile {
  margin-top: 20px;
}
.Profile-title {
  font-size: 30px;
}
.Profile-desc {
  height: 100px;
  padding: 20px;
  margin-top: 40px;
  box-shadow: 0 0 10px 2px rgba(0, 0, 0, 0.151);
  border-radius: 5px;
  box-sizing: border-box;
}