@import './Vars'

.experience
  grid-area: 3 / 1 / 4 / 5
  background-color: $back-color
  color: $text-color
  border-radius: $main-border
  box-shadow: $shadow
  display: flex
  justify-content: space-evenly;
  align-items: center