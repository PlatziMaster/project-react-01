.About
  // display grid
  // grid-template-columns repeat(2, 1fr)
  // grid-template-rows repeat(2, auto)
  padding 10px 12px
  line-height 24px
  &-title,
  h3
    // grid-area 1 / 1 / 2 / 4
    margin 0
    padding 10px
  &-contact
    padding-left 20px
  &-item
    line-height 1.5