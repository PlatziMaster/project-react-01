.skill
  display: flex
  width: 70%
  justify-content: space-between
  align-items: center
  margin-top: .5em
  .skill-container
    display flex
    flex-direction: column
    justify-content center
    align-items: flex-start
    .skill-name
      font-size: 1.5em
  .skill-image
      border-radius: 50%
